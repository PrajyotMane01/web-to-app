# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# Gradle
.gradle/
.kotlin/
/build/
/app/build/
local.properties

# Firebase — written by CI at build time from the GOOGLE_SERVICES_JSON repo
# secret (base64-decoded), never committed. See .github/workflows/build-apk.yml.
app/google-services.json

# Signing / build outputs
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.apk
*.aab

# macOS
.DS_Store
*.pem
node_modules
