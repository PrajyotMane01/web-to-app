<?xml version="1.0" encoding="utf-8"?>
<!-- Rewritten in full by CI per build (see .github/workflows/build-apk.yml,
     "Patch app name and icon") to point foreground at the downloaded
     per-app icon (@drawable/ic_launcher_foreground) instead of the system
     default used here. -->
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@color/ic_launcher_background" />
    <foreground android:drawable="@android:drawable/sym_def_app_icon" />
</adaptive-icon>
